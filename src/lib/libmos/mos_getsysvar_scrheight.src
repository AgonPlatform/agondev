	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _getsysvar_scrheight
_getsysvar_scrheight:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld	hl,0
	ld	l, (iy+sysvar_scrHeight)	; get current screenHeight
	ld	h, (iy+sysvar_scrHeight+1)
	ret
