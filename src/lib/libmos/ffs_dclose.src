    .assume    adl =1
    .include "agon/mos.inc"
    .section .text
    .global  _ffs_dclose
_ffs_dclose:
    pop     de
    ex      (sp), hl              ; pointer to DIR handle
    push    de
    ld      a,  ffs_dclose
    rst.lil 08h                   ; returns FRESULT in A

    ret        
