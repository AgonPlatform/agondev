	.assume	adl=1
	.include "agon/mos.inc"
	.section .text
	.global	_ffs_ferror
_ffs_ferror:
    pop     de
    ex      (sp), hl            ; Pointer to a FIL structure
    push    de

    ld      a,  ffs_ferror
    rst.lil 08h                 ; returns FRESULT in A

    ret
    
