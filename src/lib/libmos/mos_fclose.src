	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _mos_fclose
_mos_fclose:
  pop de
  ex (sp), hl
  push de
  ld c, l           ; filehandle in c, or 0 to close all files
	ld	a, mos_fclose
	rst.lil	08h				; returns number of files still open in A
	ret	
