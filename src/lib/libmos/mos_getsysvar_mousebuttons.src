	.assume	adl=1
	.include "agon/mos.inc"

	.global  _getsysvar_mouseButtons
	.section .text

_getsysvar_mouseButtons:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld	a, (iy+sysvar_mouseButtons)
	ld  hl,0
	ld  l, a
	ret
