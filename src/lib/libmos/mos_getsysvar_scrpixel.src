	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _getsysvar_scrpixel
_getsysvar_scrpixel:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld	hl, (iy+sysvar_scrpixel)
	ret
