	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _getsysvar_vdp_pflags
_getsysvar_vdp_pflags:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld	a, (iy+sysvar_vdp_pflags)
	ret
