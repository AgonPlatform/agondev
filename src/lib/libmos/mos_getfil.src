	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _mos_getfil
_mos_getfil:
  pop   de
  ex    (sp), hl
  push  de
  push  hl
  pop   bc          ; File identifier
	ld a,	mos_getfil
	rst.lil	08h				; Get a pointer to the relevant FIL struct
	ret
