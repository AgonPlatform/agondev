	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _getsysvar_keyrate
_getsysvar_keyrate:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld	hl, 0
	ld	l, (iy+sysvar_keyrate)
	ld	h, (iy+sysvar_keyrate+1)
	ret
