	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global	_mos_cd
_mos_cd:
  pop de
  ex (sp), hl         ; address of pathname (zero terminated)
  push de
	ld a,	mos_cd
	rst.lil	08h				; Change current directory on the SD card
	ret
