	.assume	adl=1
	.include "agon/mos.inc"

	.global  _getsysvar_mouseWheel
	.section .text

_getsysvar_mouseWheel:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld	a, (iy+sysvar_mouseWheel)
	ld hl,0
	ld l, a
	ret
