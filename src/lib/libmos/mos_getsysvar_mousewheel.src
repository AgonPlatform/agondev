	.assume	adl=1
	.include "agon/mos.inc"

	.global  _getsysvar_mouseWheel
	.section .text

_getsysvar_mouseWheel:
	push	ix
	ld	a, mos_sysvars
	rst.lil	08h
	ld	a, (ix+sysvar_mouseWheel)
	ld hl,0
	ld l, a
	pop	ix
	ret
