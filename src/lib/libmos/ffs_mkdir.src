	.assume	adl=1
	.include "agon/mos.inc"
	.section .text
	.global	_ffs_mkdir
_ffs_mkdir:
    pop     de
    ex      (sp), hl            ; Pointer to a zero-terminated directory name string
    push    de

    ld      a,  ffs_mkdir
    rst.lil 08h                 ; returns FRESULT in A

    ret
    
