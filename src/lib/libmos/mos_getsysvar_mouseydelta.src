	.assume	adl=1
	.include "agon/mos.inc"

	.global  _getsysvar_mouseYDelta
	.section .text

_getsysvar_mouseYDelta:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld hl,0
	ld	a, (iy+sysvar_mouseYDelta)
	ld l, a
	ld	a, (iy+sysvar_mouseYDelta+1)
	ld h, a
	ret
