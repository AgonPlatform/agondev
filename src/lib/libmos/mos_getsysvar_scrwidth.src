	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _getsysvar_scrwidth
_getsysvar_scrwidth:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld	hl,0
	ld	l, (iy+sysvar_scrWidth)	; get current screenwidth
	ld	h, (iy+sysvar_scrWidth+1)
	ret
