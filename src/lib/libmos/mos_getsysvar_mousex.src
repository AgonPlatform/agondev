	.assume	adl=1
	.include "agon/mos.inc"

	.global  _getsysvar_mouseX
	.section .text

_getsysvar_mouseX:
	push	ix
	ld	a, mos_sysvars
	rst.lil	08h
	ld hl,0
	ld	a, (ix+sysvar_mouseX)
	ld l, a
	ld	a, (ix+sysvar_mouseX+1)
	ld h, a
	pop	ix
	ret
