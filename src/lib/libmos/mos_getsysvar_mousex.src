	.assume	adl=1
	.include "agon/mos.inc"

	.global  _getsysvar_mouseX
	.section .text

_getsysvar_mouseX:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld hl,0
	ld	a, (iy+sysvar_mouseX)
	ld l, a
	ld	a, (iy+sysvar_mouseX+1)
	ld h, a
	ret
