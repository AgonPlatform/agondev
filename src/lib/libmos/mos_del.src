	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global	_mos_del
_mos_del:
  pop de
  ex (sp), hl       ; Address of path (zero terminated)
  push de
	ld a,	mos_del
	rst.lil	08h				; Delete a file or folder from the SD card
	ret
