	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _mos_setrtc
_mos_setrtc:
  pop   de
  ex    (sp), hl    ; Pointer to a 6-byte buffer with the time data
  push  de
	ld    a,	mos_setrtc
	rst.lil	08h				; Set the RTC (Requires MOS 1.03 or above)
	ret
