	.assume	adl=1
	.include "agon/mos.inc"

	.global  _getsysvar_mouseY
	.section .text

_getsysvar_mouseY:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld hl,0
	ld	a, (iy+sysvar_mouseY)
	ld l, a
	ld	a, (iy+sysvar_mouseY+1)
	ld h, a
	ret
