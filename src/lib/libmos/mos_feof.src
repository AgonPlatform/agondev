	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _mos_feof
_mos_feof:
  pop de
  ex (sp), hl
  push de
  ld c, l           ; filehandle
	ld	a, mos_feof
	rst.lil	08h				; returns A: 1 at End-of-File, 0 otherwise
	ret	
