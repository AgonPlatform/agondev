	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _getsysvar_rtc
_getsysvar_rtc:					; Returns pointer rather than value
  ld  iy, (_sys_vars)     ; pointer to the system variables
	lea 	hl, iy+sysvar_rtc+0
	ret
