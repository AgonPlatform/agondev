	.assume	adl=1
	.include "agon/mos.inc"

	.global  _getsysvar_mouseXDelta
	.section .text

_getsysvar_mouseXDelta:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld hl,0
	ld	a, (iy+sysvar_mouseXDelta)
	ld l, a
	ld	a, (iy+sysvar_mouseXDelta+1)
	ld h, a
	ret
