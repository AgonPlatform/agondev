	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global	_waitvblank
_waitvblank:
  ld  iy, (_sys_vars)         ; pointer to the system variables
	ld.lil	a, (iy + sysvar_time + 0)
_waitvblankloop:	
	cp.lil	a, (iy + sysvar_time + 0)
	jr	z, _waitvblankloop
	ret
