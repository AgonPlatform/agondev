	.assume	adl =1
	.include "agon/mos.inc"
	.section .text
	.global  _getsysvar_audioChannel
_getsysvar_audioChannel:
  ld  iy, (_sys_vars)     ; pointer to the system variables
	ld	a, (iy+sysvar_audioChannel)
	ret
	
