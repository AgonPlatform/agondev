	.assume	adl =1
	.include "agon/mos.inc"
	.section	.text
	.global	_putch, __putch
_putch:
__putch:
  pop     de
  ex      (sp),hl
  push    de
  ld      a,l      ;get character in A
  rst.lil 010h     ;send to MOS / VDP
  ret

